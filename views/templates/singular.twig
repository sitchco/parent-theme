{% extends 'layouts/base.twig' %}

{% block content %}
    <article {{ function('post_class', 'wp-site-blocks has-global-padding is-layout-constrained') }} id="post-{{ post.id }}">
        {{ post.content }}
    </article>
{% endblock %}

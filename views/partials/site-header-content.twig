{% set header_classes = [
    site_header.is_overlaid ? 'has-site-header-overlay',
    site_header.is_sticky ? 'has-site-header-sticky',
] %}

<header class="{{ header_classes|join(' ')|trim }}">
    {# The site-header class is applied in the CMS on the outer-most group #}
    {# TODO: consider adding js logic to apply site-header class to first element after <header> #}
    {{ site_header.content }}
</header>
<footer class="site-footer" {{ gtm_attr('Footer') }}>
    <div class="site-footer__container wp-site-blocks has-global-padding is-layout-constrained">
        {% if site_footer %}
            {{ site_footer.content }}
        {% else %}
            {# Default to the Default #}
            Footer Default
        {% endif %}
    </div>
</footer>